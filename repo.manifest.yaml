# repo.manifest.yaml — How to run/verify this repository
version: 3
last_updated: "2026-01-22"

structure:
  backend: backend/
  frontend: frontend/
  scripts: scripts/
  tests: tests/

verification:
  notes: >
    Commands are repo-specific. Prefer cost-free local verification.
  recommended:
    - make setup
    - make lint
    - make test
    - make typecheck
    - make e2e
    - make dev
    - docker-compose up
